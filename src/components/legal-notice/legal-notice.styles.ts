import styled from "styled-components";

export const LegalNoticeWrapper = styled.div`
  position: absolute;
  top: calc(1319px * var(--scale-y));
  left: calc(136px * var(--scale-x));
  color: white;

  svg {
    width: calc(551.2px * var(--scale-x));
  }
`;
